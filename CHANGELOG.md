# CHANGELOG

このファイルは、LightingPlayer の詳細な更新履歴を記録します。

## 更新履歴

2026/05/03
- バグ修正：個別制御の通常動作で、制御先ドロップダウンの選択が実行中エフェクトの対象へ影響していた問題を修正。制御先は設定対象の切り替え専用とし、保存済みのゾーン別設定が起動時・切り替え後とも正しく動作するよう変更。
- バグ修正：個別制御のプレビューが制御先選択時点で始まっていた問題を修正。プレビューは「設定」から開いた設定画面の表示中のみ有効となり、閉じると終了するよう変更。
- バグ修正：`app-settings.json` が存在しない初回起動時、音声解析を全帯域 OFF・制御スコープを一括で開始し、OpenRGB モード関連設定を安全な初期状態で起動するよう変更。
- バグ修正：OpenRGB コントローラー未検出時にデバイスモード制御へ入らないよう修正し、一括制御へ自動フォールバックするよう変更。
- バグ修正：設定リセット後に設定ファイル保存と再読込を実行し、起動時と同じ読込経路で状態を再同期するよう変更。
- バグ修正：起動時のシステム音声接続失敗時、エラーダイアログ表示を抑制してメインウィンドウ表示を継続するよう変更。
- ドキュメント更新。

2026/05/02
- バグ修正：一部コントローラーで `Wave` 系デバイスモードから一括 / 個別の通常エフェクトへ戻る際、`SetCustomMode` と通常描画送信が届いていても主導権競合が残る問題を修正。`Wave` 系モード離脱時は一度同一コントローラーの非 `Wave` モードへ遷移してから通常エフェクトへ戻すよう変更。
- バグ修正：デバイスモードの保存済みモード名を候補更新時の復元判定へ優先利用するよう変更し、再起動後に先頭候補へ誤って切り替わる問題を修正。
- 仕様変更：OpenRGB の `Direct` / ダイレクト系モードをデバイスモード候補に含め、選択・保存・復元できるよう変更。
- バグ修正：デバイスモードから別の発光モードへ切り替えて戻した際、色編集不可の一時状態で空のモード色配列を保存してしまい、モード色設定が白にリセットされ、そのまま終了すると次回起動時も白になる問題を修正。
- バグ修正：デバイスモード起動復元時に、空のモード色配列を白補完した結果が保存設定へ書き戻され、一部ゾーンが `255,255,255` で発光していた問題を修正。
- バグ修正：デバイスモード起動復元時に、コントローラー別モード / 色設定の復元完了前にデバイスモードが適用され、一部ゾーンへ正しい色設定が届かない問題を修正。復元完了後に適用するよう変更。
- バグ修正：個別制御の通常動作で、選択中ではないゾーンの保存済みエフェクトが同時実行され、ランダムフラッシュと選択エフェクトが競合して見える問題を修正。
- 個別制御時の通常動作は選択中ゾーンのみを実行し、設定画面プレビュー時のみプレビュー対象ゾーンを優先する現行仕様へ説明を更新。

2026/05/01
- 制御スコープに「デバイスモード」を追加し、ライティング制御画面内でコントローラ選択・モード一覧・適用を行えるよう変更。
- 色指定対応のデバイスモードで、モード色設定とカラーピッカーを表示し、OpenRGB のモード固有色を設定できるよう変更。
- デバイスモードの「更新」ボタンに、現在選択中コントローラのモード一覧を再取得するツールチップを追加。
- デバイスモード色設定の送信処理を調整し、OpenRGB の `color_mode` を mode-specific color として送信するよう変更。
- OpenRGB サーバ接続に失敗した場合、自動的に Dynamic Lighting へフォールバック接続するよう変更。
- OpenRGB / Dynamic Lighting の両方に接続できない場合、警告表示後に RGB 出力を抑制し、アプリは継続動作するよう変更。
- メイン画面とライティング制御画面に「制御ゾーン選択」を追加。OpenRGB サーバから取得できたゾーンの中から制御対象ゾーンを選択し、選択ゾーンに属する LED のみを制御できるよう変更。
- 制御対象ゾーン設定を `control-zone-selection.json` に保存し、次回起動時に自動適用するよう変更。未選択時は取得できた全 LED を制御対象とする。
- OpenRGB のゾーン取得処理を更新し、ARGB 名称に限定せず、サーバから取得できたゾーンを制御候補として扱うよう変更。
- 名前が空、または不可視文字のみの OpenRGB ゾーンを存在しないものとして扱い、制御候補・表示候補から除外するよう変更。
- OpenRGB デバイスモード一覧に、モード ID とモード名をキャッシュして表示するよう変更。途中で解析できないモードが含まれる場合も、取得済みのモード名を表示候補として保持するよう変更。
- OpenRGB 側でデバイス/ゾーン情報を取得できた場合に、おおむね制御対象として利用できる旨へ表現を変更。
- サーバ設定の反映タイミングを、入力中ではなくサーバ設定ウィンドウを閉じた時点へ変更。
- サーバ設定変更時は、変更がある場合のみ現在の接続を切断し、変更後のサーバへ再接続するよう変更。
- メイン画面に現在接続中の OpenRGB サーバ表示を追加。未接続時は「未接続」と表示。
- ライティング制御・プレイヤー・接続状況・検出状況ウィンドウに閉じるボタンを追加。
- アプリ終了時に、このアプリが起動した OpenRGB プロセスのみを終了する処理を整理。
- 接続・切断・再接続処理を整理し、手動操作時の動作を安定化。
- OpenRGB から取得したデバイス・ゾーン構成データを、解析直前の可読形式で `logs/openrgb/openrgb-readable-configuration.log` に保存するよう変更。既存の `openrgb.log` は従来どおり維持し、新しい構成ログは実行ごとに上書きする。
- バグ修正：個別制御で制御先「すべて」を選択し、設定を変更せずに設定画面を閉じた場合に、全ゾーンへ設定が反映される問題を修正。
- 個別制御で LED が 1 つのゾーンを選択した場合、移動を前提とするエフェクト（スキャナー・サイクル・パトロール）を設定画面の選択肢から除外するよう変更。
- 個別制御で特定のゾーンを選択している場合、デバイスモード一覧をそのゾーンのコントローラのモードのみに絞り込むよう変更。一括制御またはゾーン「すべて」選択時は全コントローラのモードを表示。
- デバイスモードの適用を、モード表示名が特定するコントローラにのみ行うよう変更。
- ライティング制御画面から仕様外の制御ゾーン選択ボタンを削除し、制御ゾーン選択はメイン画面から行う構成へ整理。
- デバイスモード専用設定画面を追加。コントローラー選択、モード一覧、適用、閉じる操作を分離。
- デバイスモード一覧から Direct / ダイレクト系モードを除外し、選択可能なモードがないコントローラーを非表示にするよう変更。
- デバイスモード適用中のコントローラーを通常エフェクト送信対象から除外し、別エフェクトが明示的に設定された時点で通常動作へ復帰するよう変更。
- OpenRGB モード解析を補正し、一部の 0 色モード後に続くモード名が欠落・ずれる問題を修正。
- ドキュメント更新。

2026/04/28
- ゾーン別制御 UI を更新。個別制御時のみ制御先ドロップダウンを表示し、設定ボタンは常時表示のまま一括/個別の両方から直接設定画面を開けるよう変更。
- ゾーン別ライティング設定画面を更新。共通設定と固有設定を 1 画面へ統合し、「適用」「閉じる」の確定/破棄動作を追加。
- 個別制御で制御先が「すべて」の場合、設定画面で確定した内容を全ゾーンへ反映する挙動を追加。
- リアクティブパルス設定を更新。従来のアッテネータを「光量アッテネータ」へ改め、追加で「入力アッテネータ」を設定できるよう変更。
- スキャナー設定を更新。トレイル幅の設定項目を削除し、速度・待機輝度のみを調整項目として案内するよう変更。
- ドキュメントを現行のリアクティブパルス設定仕様に合わせて更新。
- ドキュメントを現行のスキャナー設定仕様に合わせて更新。

2026/04/27
- リアクティブパルスの挙動を更新。反応判定を維持したまま、光量計算へ時間平滑化を追加し、無音移行時も下降エフェクトで自然に減衰するよう調整。
- リアクティブパルスの低域専用調整を更新。低域の反応認識を専用パラメーターで制御し、低域のみ選択時の発光量を専用比率で制御するよう変更。
- 音声解析の内部更新頻度を引き上げ、UI 反映は従来相当の頻度へ間引くよう整理。
- スキャナーの挙動を更新。無音時は片道 4 秒、移動方向の後ろ側だけに残光が残る片側トレイル、白熱球風の減衰、折り返し時の端停止、速度・トレイル幅・待機輝度の専用設定を追加。
- `docs` 配下の技術資料を現行構成へ整理し、統合版・インデックス・README の記述を一致させた。
- README の音声解析説明を、ユーザー向けの表現へ整理。

2026/04/25
- リアクティブパルスの内部動作を調整。
- メインウィンドウの音声解析レベル表示を修正し、設定画面で最後に選択した帯域状態に依存せず、低域・中域・高域・平均を常時表示するよう統一。
- 閾値設定仕様を固定化。

2026/04/24
- 音声同期ボタンを廃止。音声同期は「システム音声を解析」チェックボックスの状態と接続状態に依存するよう変更。接続時に自動で同期が有効になる。
- ランダム以外のパターン（リアクティブパルス・スキャナー・サイクル）でランダム色モードを選択した際、各設定画面およびインラインパネルにパレット色数セレクターを追加。
- プレビュー時にタイマー駆動パターン（スキャナー・サイクル・パトロール・クリスマス）が音声同期状態に依存せず動作するよう修正。
- エフェクト名称を統一。UI 表記を「リアクティブパルス」に変更し、内部表記（クラス名・識別子・設定ウィンドウ名）を `ReactivePulse` に統一。
- 音声反応評価ロジックを更新。
- ランダム / リアクティブパルス / スキャナー設定に、非排他的な 3 チャンネル選択（低域・中域・高域）を追加。
- 各設定画面のチェックボックス下へ「強さアッテネータ」スライダー（0〜100%）と ±1 / ±5 / ±10 調整ボタンを追加し、最終出力直前で `attenuatorFactor` を適用する仕様を反映。
- ゾーン設定プレビュー中の適用経路を、終了後と同じ個別ゾーン経路へ統一。プレビュー時と通常時での評価・反映ルート差異を解消。

2026/04/23
- クリスマスモードを追加。白熱電球風フェード（S字フェードイン / 指数フェードアウト）、20個単位の分散グループ、30%抽選、点灯3秒±1秒・抽選5秒±1秒の独立周期、共通8色パレット（黒・白除外6色）をREADMEへ反映。
- 実装修正：Dynamic Lighting を全デバイス一括制御に統一。
- OpenRGB 条件（ゾーン別独立制御 / LEDごとランダム / デバイスモード）を接続状態連動で厳格化。
- OpenRGB 実行バイナリはプロジェクト同梱版を優先し、既存プロセスのアタッチ先も同梱バイナリ一致に限定。
- 未使用の Dynamic Lighting 単体デバイス送信コマンドを削除。
- パトロールモード：通常時パトカー最大数を 3 → 2 に変更。
- パトカーは必ず互いに逆方向へ移動するよう修正。
- バグ修正：`AverageLevelWeight` スライダー UI 追加
- `AverageLevelWeightControlVisibility` プロパティ追加
- 「ゾーンが検出されていません」メッセージ表示ロジック修正（`LightingControlWindow.xaml` Visibility バインド競合解消）

2026/04/22
- 内部コメント整備（各モジュール主要関数への日本語コメント追加）